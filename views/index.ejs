<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogify</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <%- include("header.ejs") %>

    <main class="welcome">
        <h1> Welcome to your Blogs</h1>
        <p> Your own space to write & share. </p>

       <% if(posts == 0) { %>
            <p class="no-posts">No posts yet. Start by creating one!</p>
            <a href="/newpost" class="new-post-btn">Create New Post</h1>
       <% }else{ %>
            <ul>
                <% posts.forEach(post => { %>
                    <l1>
                        <h2><%= post.title %></h2>
                        <p><%= post.content.substring(1,100) %></p>
                        <a href="/post/<%= post.id %>">Read more</a>
                    </l1>
                <% }) %>
            </ul>
        <% } %>



    </main>

    <%- include("footer.ejs") %>

</body>
</html>





